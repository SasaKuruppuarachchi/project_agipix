attach: true
root: ./
startup_window: mocap
windows:
  - mocap:
      layout:
      panes:
        - sorpx4 && ros2 launch mocap_optitrack mocap.launch.py
            namespace:=optitrack
            config_file:=real_config/mocap.yaml
